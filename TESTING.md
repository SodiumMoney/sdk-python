# Testing

CI verifies mainnet vectors:

| Handle | Expected normalization |
|--------|------------------------|
| `@blknoiz06` | `blknoiz06` |
| `Ansem 🐂🀄️` | `ansem 🐂🀄️` |

Telegram handles use the same derivation with `platform = telegram`.
